# My Firebase Python Project

This project demonstrates how to batch write data to Firebase Firestore using Python.

## Setup

1. Clone the repository.
2. Install dependencies:
3. Generete new private key (Firebase Project -> Project settings -> Services accounts)

```bash
pip install -r requirements.txt
```
